{
  "name": "nodejs-prometheus",
  "version": "1.0.0",
  "engines": {
    "node": ">=18"
  },
  "description": "This repository contains a Node.js application that is monitored using Prometheus. The setup includes a Helm chart for deploying the application and a `ServiceMonitor` for Prometheus to scrape metrics from the application.",
  "main": "index.js",
  "scripts": {
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.0",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "supertest": "^7.2.2"
  }
}
